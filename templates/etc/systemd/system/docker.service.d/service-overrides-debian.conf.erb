[Service]
EnvironmentFile=-/etc/default/docker
EnvironmentFile=-/etc/default/docker-storage
ExecStart=
ExecStart=/usr/bin/<%= @docker_command %> <%= @daemon_flag %> $OPTIONS \
        $DOCKER_STORAGE_OPTIONS
