description     "Run docker"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn

script
  /usr/bin/docker -d <% if tcp_bind %>-H <%= @tcp_bind %><% end %> -H <%= @unix_socket %>
end script
